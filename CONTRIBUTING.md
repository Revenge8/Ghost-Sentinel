## How to contribute to Ghost Sentinel
1. Fork the Project.
2. Create your Feature Branch (`git checkout -b feature/NewDetection`).
3. Commit your Changes (`git commit -m 'Add some NewDetection'`).
4. Push to the Branch (`git push origin feature/NewDetection`).
5. Open a Pull Request.
